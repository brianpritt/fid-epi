var express = require('express')
var router = express.Router();
var Attendance = require('../models/attendance-model').Attendance;

router.get('/:id', function(req, res){

})

router.put('/:date', function(res, req){
  
})
module.exports = router;
